# Secrets — never commit
.env

# Agent-facing scaffolding — kept local, not part of the published project.
# CLAUDE.md is build instructions; PROJECT_CONTEXT.md is a recap of the design
# conversation. Both are internal working docs. PRD.md is the public spec.
CLAUDE.md
PROJECT_CONTEXT.md

# Personal listener profile. This is user-editable state, not code (see CLAUDE.md).
# Ship prompts/skills/explainer-podcast/references/listener.example.md instead;
# `zonecast profile init` copies it into place on first run.
prompts/skills/explainer-podcast/references/listener.md

# Generated episode artifacts — audio, drafts, manifests
episodes/*
!episodes/.gitkeep

# Runtime-distilled field style guides (FR-10 cache, regenerated on demand)
styles/*
!styles/.gitkeep

# Published feed
feed.xml

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# macOS
.DS_Store

# Working notes from the hand-run test pass. Findings are folded into PRD.md and the
# skill files, which are canonical; this is the historical record and contains claims
# (word counts, "[APPLIED]" markers) that did not survive verification.
TIGHTENING_NOTES.md

# GSD planning docs — local only (git tracking disabled at init)
.planning/
